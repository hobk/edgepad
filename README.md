# 📝 EdgePad Global Sync

> 一个运行在边缘节点（Edge Function）上的无服务器、即时协同富文本便签应用。无需源站数据库，利用 EdgeKV 实现全球数据分发。

![Platform](https://img.shields.io/badge/Platform-Edge_Runtime-blue)
![Database](https://img.shields.io/badge/Storage-EdgeKV-green)
![License](https://img.shields.io/badge/License-MIT-orange)

## ✨ 项目简介 (Introduction)

**EdgePad** 是一个极其轻量级的在线笔记本。与传统应用不同，它没有传统的后端服务器（Node/Python/Java），所有的逻辑（路由、渲染、存储）都由 **边缘函数 (Edge Function)** 处理，数据存储在分布式的 **Key-Value Store (EdgeKV)** 中。

这意味着无论用户在世界的哪个角落，都能享受到极低延迟的访问体验，并且内容会最终一致地同步到全球所有节点。

## 🚀 核心特性 (Features)

* **🌍 全球同步**：基于 EdgeKV，一次写入，数秒内分发至全球边缘节点。
* **🖼️ 富文本支持**：
    * 支持像 Word 一样的所见即所得编辑。
    * **智能图片压缩**：支持直接粘贴 (`Ctrl+V`) 截图或上传图片。前端自动利用 Canvas 将图片压缩至 JPEG (Max width 800px)，以适应 KV 存储限制。
* **🔗 极速分享**：
    * **智能短 ID**：自动生成 3-5 位随机短 ID (如 `/x9z`)。
    * **二维码生成**：内置悬浮二维码，方便手机扫码同步。
    * **一键复制**：兼容 HTTPS (Clipboard API) 和 HTTP (execCommand 回退方案)。
* **💾 自动保存**：内置防抖 (Debounce) 机制，停止输入 1 秒后自动同步，减少请求次数。
* **🛡️ 强鲁棒性**：
    * 处理存储体积超限 (1.8MB) 的情况并给出强提醒。
    * 防止空内容写入导致的数据库错误。

## 🛠️ 技术栈 (Tech Stack)

* **Runtime**: Edge Worker / Edge Function (Standard Fetch API)
* **Storage**: EdgeKV (Key-Value Database)
* **Frontend**: Native HTML5 + CSS3 + Vanilla JavaScript
* **Libs**: `qrcode.js` (CDN引入)

## 📦 部署指南 (Deployment)

### 1. 前置准备
确保你的边缘计算平台（如 Akamai, Cloudflare 等）已开启 **EdgeKV** 服务。

### 2. 初始化数据库
创建一个命名空间（Namespace），本项目默认配置如下：
* **Namespace**: `ns`
* **Group**: `default`

> 如果使用不同的名称，请修改代码中的 `const edgeKV = new EdgeKV({ namespace: 'ns' });`。

### 3. 上传代码
将 `main.js` (您的边缘函数入口文件) 部署到平台。

### 4. 绑定路由
建议将路由绑定到根路径 `/*`，以确保能够拦截 `/api/save` 和短链接跳转。

## 📖 使用说明

1.  **创建笔记**：
    * 访问首页（如 `https://your-domain.com/`），系统会自动跳转到一个随机生成的短链接（如 `/a1b2`）。
    * 或者手动输入自定义 URL（如 `https://your-domain.com/meeting-notes`）。
2.  **编辑内容**：
    * 直接输入文字。
    * 截图后直接粘贴，或点击顶部 "📷 插图" 按钮上传。
3.  **保存**：
    * 无需手动操作，停止输入 1 秒后顶部状态灯变绿即表示保存成功。
4.  **分享**：
    * 点击 "📱 二维码" 供手机扫描。
    * 点击 "🔗 复制链接" 发送给同事。

## ⚠️ 限制与注意事项 (Constraints)

* **存储限制 (1.8MB)**：
    * 由于 EdgeKV 单个 Value 的物理限制，笔记总大小（含图片 Base64 编码）不能超过 **1.8MB**。
    * *建议*：一篇笔记中最好不要超过 **3-5 张** 图片。如果超限，系统会弹出红色警告。
* **最终一致性**：
    * 数据同步到全球所有边缘节点通常需要 **3-5 秒**。在极短时间内，不同地区的用户可能会看到旧版本。
* **隐私安全**：
    * **所有链接均由 ID 唯一确定且公开**。任何知道 URL 的人都可以访问和编辑。
    * **请勿**在笔记中保存密码、私钥、API Key 等敏感信息。

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request 来改进图片压缩算法或 UI 体验。

## 📄 License

MIT License

> 本项目由阿里云ESA提供加速、计算和保护 ![img](https://img.alicdn.com/imgextra/i3/O1CN01H1UU3i1Cti9lYtFrs_!!6000000000139-2-tps-7534-844.png)
