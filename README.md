# EdgePad - 基于边缘函数的图文记事本

EdgePad 是一个现代化的图文记事本应用，基于边缘函数（Edge Functions）构建，支持文本和图片内容。

## ✨ 特性

- 📝 **图文并茂**：支持文本和图片混合编辑
- ⚡ **边缘计算**：使用 Cloudflare Workers/Pages 边缘函数，全球快速访问
- 💾 **自动保存**：编辑内容自动保存，无需手动操作
- 📱 **响应式设计**：支持桌面和移动设备
- 🎨 **现代界面**：简洁美观的用户界面
- 🔒 **本地优先**：支持 localStorage 本地存储（开发模式）

## 🚀 快速开始

### 本地开发

1. 克隆仓库
```bash
git clone https://github.com/hobk/edgepad.git
cd edgepad
```

2. 安装依赖
```bash
npm install
```

3. 启动开发服务器
```bash
npm run dev
```

4. 打开浏览器访问 `http://localhost:8788`

### 部署到 Cloudflare Pages

1. 安装 Wrangler CLI（如果还没有）
```bash
npm install -g wrangler
```

2. 登录 Cloudflare 账号
```bash
wrangler login
```

3. 创建 KV 命名空间（用于存储笔记数据）
```bash
wrangler kv:namespace create NOTES_KV
```

4. 在 `wrangler.toml` 中添加 KV 绑定（将 ID 替换为上一步创建的 ID）
```toml
[[kv_namespaces]]
binding = "NOTES_KV"
id = "your_kv_namespace_id"
```

5. 部署应用
```bash
npm run deploy
```

## 📁 项目结构

```
edgepad/
├── public/                    # 静态文件目录
│   ├── index.html            # 主页面
│   ├── styles.css            # 样式文件
│   ├── app.js                # 应用逻辑
│   └── functions/            # 边缘函数
│       ├── _middleware.js    # 中间件
│       └── api/
│           ├── notes.js      # 笔记列表 API
│           └── notes/
│               └── [id].js   # 单个笔记 API
├── package.json              # 项目配置
├── wrangler.toml            # Cloudflare 配置
└── README.md                # 说明文档
```

## 🔧 技术栈

- **前端**：HTML5, CSS3, Vanilla JavaScript
- **后端**：Cloudflare Workers/Pages Functions
- **存储**：Cloudflare KV (生产环境) / localStorage (开发环境)
- **部署**：Cloudflare Pages

## 📖 使用说明

1. **创建笔记**：点击左侧栏的"新建"按钮
2. **编辑笔记**：在右侧编辑区域输入标题和内容
3. **添加图片**：点击"图片"按钮选择图片文件
4. **保存笔记**：内容会自动保存，也可以手动点击"保存"按钮
5. **删除笔记**：点击"删除"按钮并确认

## 🌐 API 接口

### 获取所有笔记
```
GET /api/notes
```

### 创建新笔记
```
POST /api/notes
Content-Type: application/json

{
  "title": "笔记标题",
  "content": "笔记内容",
  "images": []
}
```

### 获取单个笔记
```
GET /api/notes/:id
```

### 更新笔记
```
PUT /api/notes/:id
Content-Type: application/json

{
  "title": "更新的标题",
  "content": "更新的内容",
  "images": ["data:image/png;base64,..."]
}
```

### 删除笔记
```
DELETE /api/notes/:id
```

## 💡 开发模式

在本地开发时，应用会自动检测是否有可用的 API 后端。如果没有，会自动切换到 localStorage 模式，所有数据都存储在浏览器本地。

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

